// Package config defines typed application configuration structures and
// validation helpers.
//
// It contains reusable app config models, validation methods, and the
// mapstructure decode hook used to load scalar types from text-based
// configuration sources.
package config
